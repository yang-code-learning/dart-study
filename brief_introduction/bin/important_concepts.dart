/* 
 * 1. Everything you can place in a variable is an object, 
 * and every object is an instance of a class
 * 2. Although Dart is strongly typed, type annotations 
 * are optional because Dart can infer types.
 * ...
 */